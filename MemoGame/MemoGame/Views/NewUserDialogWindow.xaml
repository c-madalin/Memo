<UserControl x:Class="MemoGame.Views.NewUserDialogWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MemoGame.Views"
             xmlns:vm="clr-namespace:MemoGame.ViewModels"
             mc:Ignorable="d"
             Height="380" Width="430">

    <UserControl.DataContext>
        <vm:NewUserDialogViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Width" Value="130"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#4A4A4A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A5A5A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Border BorderThickness="1" BorderBrush="#555555" CornerRadius="8" Background="#3A3A3A">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title with close button -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock 
                    Text="Create New User" 
                    FontSize="22" 
                    FontWeight="Bold"
                    HorizontalAlignment="Center"/>
                <Button 
                    Content="✕" 
                    Click="CloseButton_Click"
                    Background="Transparent" 
                    Foreground="White" 
                    BorderThickness="0"
                    FontSize="16"
                    Width="30"
                    Height="30"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,-5,0,0"/>
            </Grid>

            <!-- Username input -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock 
                    Text="Username:" 
                    FontSize="16"
                    Margin="0,0,0,8"/>
                <TextBox 
                    Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                    Height="34"
                    VerticalContentAlignment="Center"/>
            </StackPanel>

            <!-- Avatar selection -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                <TextBlock 
                    Text="Select Avatar:" 
                    FontSize="16"
                    Margin="0,0,0,15"
                    HorizontalAlignment="Center"/>
                <Border 
                    Width="180" 
                    Height="180" 
                    Background="#2A2A2A" 
                    CornerRadius="90"
                    Margin="0,0,0,20">
                    <Image 
                        Source="{Binding SelectedAvatar}" 
                        Stretch="UniformToFill"/>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button 
                        Content="Previous" 
                        Command="{Binding PreviousAvatarCommand}" 
                        Style="{StaticResource MenuButtonStyle}"
                        Width="110"
                        Margin="5,0"/>
                    <Button 
                        Content="Next" 
                        Command="{Binding NextAvatarCommand}" 
                        Style="{StaticResource MenuButtonStyle}"
                        Width="110"
                        Margin="5,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Action buttons -->
            <StackPanel 
                Grid.Row="3" 
                Orientation="Horizontal" 
                HorizontalAlignment="Center"
                Margin="0,20,0,0">
                <Button 
                    Content="Create" 
                    Click="CreateButton_Click" 
                    Style="{StaticResource MenuButtonStyle}"
                    IsEnabled="{Binding CanCreate}"/>
                <Button 
                    Content="Cancel" 
                    Click="CancelButton_Click" 
                    Style="{StaticResource MenuButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>